.container.d-flex.flex-column.justify-content-center.align-items-center.c-content-area
  .w-100.mt-5.mb-2.d-flex.justify-content-between.align-items-center
    h2.inline-block #{JSON.parse(categoryInfo).categoryDescription}
    a.btn.btn-outline-info.inline-block.align-self-end(href="/board/p") 새 글쓰기
  table.table.table-hover
    thead
      tr
        th(scope='col') #
        th(scope='col') 제목
        th(scope='col') 작성자
        th(scope='col') 작성일
        th(scope='col') 조회수
    tbody
      each boardInfo in JSON.parse(boardList)
        +__boardListElement(boardInfo)
  nav.my-4
    ul.pagination 
      +__boardListPagination(JSON.parse(pageInfo),JSON.parse(categoryInfo).categoryId)

